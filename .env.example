# Local Postgres — `docker run -d --name inksign-pg -p 5432:5432 -e POSTGRES_PASSWORD=dev postgres:16`
DATABASE_URL=postgres://postgres:dev@localhost:5432/postgres

# Port the Bun server listens on. Render sets this itself.
PORT=3000
